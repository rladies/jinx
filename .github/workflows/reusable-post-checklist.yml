<<<<<<< HEAD:.github/workflows/reusable-post-checklist.yml
name: Reusable · Post Checklist
=======
# Reusable: posts the blog-review checklist on a PR that touches
# blog content. The caller is expected to gate this with a `paths:`
# filter so it only fires on relevant changes.
#
# Caller requirements:
#   - permissions: contents: read, packages: read  (needed to pull
#     ghcr.io/rladies/jinx-bot — without packages: read the docker
#     pull fails with "denied")
#   - secrets: JINX_APP_ID, JINX_PRIVATE_KEY (both available as
#     rladies org-level secrets)
#   - jinx[bot] GitHub App installed on the calling repo
#
# See README.md ("Reusable workflows") for the full caller example.
name: Reusable · Website Blog Checklist
>>>>>>> origin/main:.github/workflows/reusable-website-blog-checklist.yml

on:
  workflow_call:
    secrets:
      JINX_APP_ID:
        required: true
      JINX_PRIVATE_KEY:
        required: true

permissions:
  contents: read
  packages: read

jobs:
  checklist:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/rladies/jinx-bot:latest
    steps:
      - name: Generate app token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.JINX_APP_ID }}
          private-key: ${{ secrets.JINX_PRIVATE_KEY }}
          owner: rladies

      - name: Post content checklist
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          Rscript -e '
            jinx::gh_post_checklist(
              owner = "${{ github.repository_owner }}",
              repo = "${{ github.event.repository.name }}",
              pr_number = ${{ github.event.pull_request.number }}
            )
          '
