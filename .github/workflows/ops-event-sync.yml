name: Ops · Event Sync

on:
  schedule:
    - cron: "30 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Generate app token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.JINX_APP_ID }}
          private-key: ${{ secrets.JINX_PRIVATE_KEY }}
          owner: rladies

      - name: Checkout jinx
        uses: actions/checkout@v6
        with:
          repository: rladies/jinx

      - name: Setup R
        uses: r-lib/actions/setup-r@v2

      - name: Install dependencies
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: local::.

      - name: Sync and publish events
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          MEETUP_API_KEY: ${{ secrets.MEETUP_API_KEY }}
          MEETUPR_JWT_TOKEN: ${{ secrets.MEETUPR_JWT_TOKEN }}
          MEETUPR_CLIENT_ID: ${{ secrets.MEETUPR_CLIENT_ID }}
          MEETUPR_JWT_ISSUER: ${{ secrets.MEETUPR_JWT_ISSUER }}
        run: |
          Rscript -e '
            events <- jinx::events_sync_chapters(dry_run = FALSE)
            summary <- jinx::events_create_summary(events, "weekly")
            jinx::events_publish_summary(summary)
          '
