{
  "name": "jinx-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker bridging Slack and GitHub for the Jinx R package. Not published; package.json exists only for local testing.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
