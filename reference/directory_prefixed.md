# Collapse `<prefix>*` scalar fields into a named list without the prefix.

Collapse `<prefix>*` scalar fields into a named list without the prefix.

## Usage

``` r
directory_prefixed(fields, prefix)
```
