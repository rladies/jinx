# Point a branch at a commit, creating the ref or force-updating it.

Point a branch at a commit, creating the ref or force-updating it.

## Usage

``` r
directory_set_branch(org, repo, branch, sha)
```
