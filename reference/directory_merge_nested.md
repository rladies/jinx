# Overlay a nested object's sub-keys, or replace wholesale if not both lists.

Overlay a nested object's sub-keys, or replace wholesale if not both
lists.

## Usage

``` r
directory_merge_nested(target, val)
```
