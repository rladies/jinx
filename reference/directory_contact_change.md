# Resolve a contact email into a file change, or `NULL` if unchanged.

Resolve a contact email into a file change, or `NULL` if unchanged.

## Usage

``` r
directory_contact_change(slug, email, org, repo, ref)
```
