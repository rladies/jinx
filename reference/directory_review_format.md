# Format the per-entry review results as a markdown PR checklist comment.

Each entry is a task-list checkbox reviewers tick as they go, so
progress is visible at a glance; automated flags are shown under their
entry.

## Usage

``` r
directory_review_format(reviews)
```
