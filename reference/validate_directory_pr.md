# Post a directory validation report as a PR comment

Runs all directory validations (schema, filenames, social handles) on
changed files in a PR and posts a summary comment.

## Usage

``` r
validate_directory_pr(owner, repo, pr_number)
```

## Arguments

- owner:

  Repository owner.

- repo:

  Repository name.

- pr_number:

  PR number.
