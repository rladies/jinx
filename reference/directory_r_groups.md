# Build the `r_groups` name -\> url map from `rgroup_name_*`/`rgroup_url_*`.

Build the `r_groups` name -\> url map from
`rgroup_name_*`/`rgroup_url_*`.

## Usage

``` r
directory_r_groups(fields)
```
