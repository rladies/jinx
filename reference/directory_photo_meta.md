# Extract photo download metadata (url, extension, credit) from a submission.

Extract photo download metadata (url, extension, credit) from a
submission.

## Usage

``` r
directory_photo_meta(fields)
```
