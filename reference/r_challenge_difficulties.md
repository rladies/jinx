# Difficulty levels for the weekly R challenge

Difficulty levels for the weekly R challenge

## Usage

``` r
r_challenge_difficulties()
```

## Value

Data frame with columns `level`, `label`, and `badge` (a Slack emoji),
one row per difficulty.
