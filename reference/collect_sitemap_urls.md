# Walk a sitemap (and any nested sitemap-index) and return all URLs

Walk a sitemap (and any nested sitemap-index) and return all URLs

## Usage

``` r
collect_sitemap_urls(url, depth = 0L, max_depth = 2L)
```
