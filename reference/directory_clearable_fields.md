# Fields the form may wipe via `clear_fields`. `name` is intentionally absent.

Fields the form may wipe via `clear_fields`. `name` is intentionally
absent.

## Usage

``` r
directory_clearable_fields()
```
