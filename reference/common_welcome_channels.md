# Channels common to both RLadies+ Slack workspaces

Channels that exist in both the community and organisers Slack
workspaces, and are useful to surface to any new member regardless of
which workspace they joined.

## Usage

``` r
common_welcome_channels()
```

## Value

A list of named character vectors with `name` and `desc`.
