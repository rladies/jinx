# Fetch one awesome-creations feed and format each item into a chunk

Fetch one awesome-creations feed and format each item into a chunk

## Usage

``` r
gather_awesome_feed(feed, src, min_chars)
```
