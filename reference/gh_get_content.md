# GET a repo file's contents object, or `NULL` if it does not exist.

GET a repo file's contents object, or `NULL` if it does not exist.

## Usage

``` r
gh_get_content(org, repo, path, ref = "main")
```
