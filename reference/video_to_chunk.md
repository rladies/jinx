# Convert a YouTube playlistItem to a chunk, skipping private/deleted videos

Convert a YouTube playlistItem to a chunk, skipping private/deleted
videos

## Usage

``` r
video_to_chunk(item, src, max_description_chars)
```
