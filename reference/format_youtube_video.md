# Format a YouTube video's title, publish date, and description as chunk text

Format a YouTube video's title, publish date, and description as chunk
text

## Usage

``` r
format_youtube_video(title, published_at, description)
```
