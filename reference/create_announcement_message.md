# Create a formatted announcement message

Create a formatted announcement message

## Usage

``` r
create_announcement_message(frontmatter, uri)
```

## Arguments

- frontmatter:

  Named list from YAML front matter (needs `title`, `description`,
  `tags`).

- uri:

  URL of the post.

## Value

Formatted message string.
