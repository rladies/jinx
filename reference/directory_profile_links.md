# Build a "Profiles" line of clickable social links for the reviewer.

The social platforms block automated existence checks, so instead of an
unreliable HTTP probe the review offers ready-made links the reviewer
can click to confirm each account resolves.

## Usage

``` r
directory_profile_links(entry)
```
