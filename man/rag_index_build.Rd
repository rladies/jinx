% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rag-index.R
\name{rag_index_build}
\alias{rag_index_build}
\title{Build the Jinx RAG index}
\usage{
rag_index_build(
  sources = NULL,
  account_id = NULL,
  api_token = Sys.getenv("CLOUDFLARE_API_TOKEN"),
  index_name = NULL,
  batch_size = 50L,
  model = "@cf/baai/bge-base-en-v1.5"
)
}
\arguments{
\item{sources}{Optional list of source specs (see \code{load_rag_sources()}).}

\item{account_id}{Cloudflare account ID. Falls back to env
\code{CLOUDFLARE_ACCOUNT_ID}, then \code{cloudflare_account_id()}.}

\item{api_token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}

\item{index_name}{Vectorize index. Defaults to env \code{VECTORIZE_INDEX}
or \code{"rladies-content"}.}

\item{batch_size}{Number of chunks per embed/upsert call.}

\item{model}{Workers AI embedding model.}
}
\value{
Invisibly: list with \code{chunks} (total) and \code{upsert} (API response).
}
\description{
Gathers chunks from every configured source, embeds them with
Cloudflare Workers AI, and upserts the vectors into a Vectorize
index. The source list is loaded from \code{inst/config/rag-sources.yml}
unless \code{sources} is supplied directly.
}
