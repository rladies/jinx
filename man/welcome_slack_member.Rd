% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slack-welcome.R
\name{welcome_slack_member}
\alias{welcome_slack_member}
\title{Welcome a new member to an RLadies+ Slack workspace}
\usage{
welcome_slack_member(
  user_id,
  workspace = c("community", "organisers"),
  coc_url = "https://rladies.org/about/coc/",
  welcome_channel = NULL,
  help_channel = NULL,
  starter_channels = NULL,
  token = Sys.getenv("SLACK_TOKEN")
)
}
\arguments{
\item{user_id}{Slack user ID (e.g. \code{"U12345"}).}

\item{workspace}{One of \code{"community"} or \code{"organisers"}. Selects
which template and default channel set is used.}

\item{coc_url}{URL to the RLadies+ Code of Conduct.}

\item{welcome_channel}{Channel name (no \verb{#}) where new members
introduce themselves. Defaults to \code{"welcome"}.}

\item{help_channel}{Channel name (no \verb{#}) where workspace questions
can be asked. Workspace-specific default.}

\item{starter_channels}{List of starter channels, each a list with
\code{name} and \code{desc}. Defaults to \code{\link[=default_welcome_channels]{default_welcome_channels()}} for
the chosen workspace.}

\item{token}{Slack API token. Defaults to \code{Sys.getenv("SLACK_TOKEN")}.}
}
\value{
API response (invisibly).
}
\description{
Sends a direct message to the given Slack user introducing them to
the workspace, pointing them to the Code of Conduct, the welcome
channel, and a starter set of channels to explore. Two templates
are available: one for the RLadies+ Community Slack, one for the
RLadies+ Organisers Slack.
}
\details{
The function is stateless: jinx does not persist any user
identifiers. The Slack \code{team_join} event is the source of truth
for who to welcome -- see the \code{slack-welcome.yml} GitHub Actions
workflow (\code{workflow_dispatch} / \code{repository_dispatch} triggered by
the Slack event handler) for the wiring.
}
