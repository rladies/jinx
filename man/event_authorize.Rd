% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-registry.R
\name{event_authorize}
\alias{event_authorize}
\title{Authorize a parsed event before execution}
\usage{
event_authorize(
  event,
  organiser_id = Sys.getenv("SLACK_ORGANIZER_TEAM_ID"),
  community_id = Sys.getenv("SLACK_COMMUNITY_TEAM_ID")
)
}
\arguments{
\item{event}{Parsed event list from \code{\link[=event_parse]{event_parse()}}, or \code{NULL}.}

\item{organiser_id}{Organiser workspace team id. Defaults to env
\code{SLACK_ORGANIZER_TEAM_ID}.}

\item{community_id}{Community workspace team id. Defaults to env
\code{SLACK_COMMUNITY_TEAM_ID}.}
}
\value{
A list with \code{ok} (logical) and \code{message} (a refusal string
when \code{ok} is \code{FALSE}, otherwise \code{NULL}).
}
\description{
Events are gated on the originating team being an allowed workspace,
not on actor privilege (there is no "actor" for a passive event like a
reaction or a team join - anyone triggers these). \code{airtable_webhook}
events have no \code{team_id} at all (Airtable webhooks aren't Slack-team
scoped) and are trusted by construction: the Worker already verified
the webhook's shared secret before dispatching. The other three kinds
are checked against the organiser/community team allowlist, mirroring
the Worker's own \code{slack_team_is_allowed()}.
}
