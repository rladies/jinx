% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channel-promo.R
\name{channel_promo_build}
\alias{channel_promo_build}
\title{Build the weekly channel-spotlight message}
\usage{
channel_promo_build(
  team_id = Sys.getenv("SLACK_COMMUNITY_TEAM_ID"),
  target_channel = Sys.getenv("SLACK_PROMO_CHANNEL", "general"),
  skip = promo_skip_channels(),
  namespace_id = slack_tokens_namespace_id(),
  account_id = Sys.getenv("CLOUDFLARE_ACCOUNT_ID"),
  api_token = Sys.getenv("CLOUDFLARE_API_TOKEN"),
  model = workers_ai_chat_model()
)
}
\arguments{
\item{team_id}{Community Slack team id. Defaults to env
\code{SLACK_COMMUNITY_TEAM_ID}.}

\item{target_channel}{Channel the spotlight is posted to (and never
promotes). Defaults to env \code{SLACK_PROMO_CHANNEL}, falling back to
\code{"general"}.}

\item{skip}{Additional channel names to exclude. Defaults to the
comma-separated env \code{SLACK_PROMO_SKIP}.}

\item{namespace_id}{KV namespace ID for \code{SLACK_TOKENS}.}

\item{account_id}{Cloudflare account ID. Defaults to env
\code{CLOUDFLARE_ACCOUNT_ID}.}

\item{api_token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}

\item{model}{Workers AI chat model used for drafting.}
}
\value{
A list with \code{text} (the Slack message), \code{channel_id},
\code{channel_name}, and \code{recent} (the updated round-robin state to persist),
or \code{NULL} when no channel is eligible.
}
\description{
Lists the community workspace's public channels, keeps those with a
description, picks the next one round-robin (state stored in KV), and drafts
a Jinx-voiced invitation for it - falling back to the plain description if
the model call fails.
}
