% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directory-airtable.R
\name{directory_purge_submissions}
\alias{directory_purge_submissions}
\title{Erase every Airtable submission for the given directory slugs.}
\usage{
directory_purge_submissions(
  slugs,
  base_id = directory_base_id(),
  api_key = Sys.getenv("AIRTABLE_API_KEY")
)
}
\arguments{
\item{slugs}{Character vector of directory slugs to erase.}

\item{base_id}{Airtable base ID. Defaults to the directory base.}

\item{api_key}{Airtable API key. Defaults to the \code{AIRTABLE_API_KEY} env var.}
}
\value{
Character vector of deleted record ids (invisibly).
}
\description{
GDPR right-to-erasure: after the purge workflow removes a member's entry from
the directory, every \code{submissions} row that resolves to one of \code{slugs} (by
\code{directory_id}, falling back to \code{identifier}) is \strong{deleted} from Airtable so
no submitted PII remains — and so a later sync cannot re-create the entry
from a leftover row. Unlike \code{\link[=directory_mark_synced]{directory_mark_synced()}}, which only flags the
\code{synced} checkbox, this destroys the rows.
}
