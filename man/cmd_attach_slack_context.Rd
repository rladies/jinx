% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/commands.R
\name{cmd_attach_slack_context}
\alias{cmd_attach_slack_context}
\title{Attach Slack context fields to a parsed command}
\usage{
cmd_attach_slack_context(command, team_id, channel_id, channel_name)
}
\arguments{
\item{command}{Parsed command list from \code{\link[=cmd_parse]{cmd_parse()}}, or \code{NULL}.}

\item{team_id}{Slack team id the command was run from.}

\item{channel_id}{Slack channel id the command was run from.}

\item{channel_name}{Slack channel display name.}
}
\value{
\code{command} with \code{team_id}/\code{channel_id}/\code{channel_name} added, or
\code{NULL} unchanged if \code{command} was \code{NULL}.
}
\description{
A handful of dispatched commands (\code{"feedback"}, \code{"setup-channel"}) act on
the Slack workspace/channel the command was run from, rather than only on
fields parsed from the command text itself. Owning that merge here -
mirroring how \code{\link[=event_parse]{event_parse()}} owns shaping the \code{slack-event} payload -
keeps the calling workflow YAML a thin, untested plumbing layer instead
of the place command semantics live.
}
