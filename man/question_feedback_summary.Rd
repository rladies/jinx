% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/question-log.R
\name{question_feedback_summary}
\alias{question_feedback_summary}
\title{Summarize reaction-feedback tallies for a Slack workspace}
\usage{
question_feedback_summary(
  team_id,
  days = 7,
  namespace_id = slack_tokens_namespace_id(),
  account_id = Sys.getenv("CLOUDFLARE_ACCOUNT_ID"),
  api_token = Sys.getenv("CLOUDFLARE_API_TOKEN")
)
}
\arguments{
\item{team_id}{Slack team id. Required (aborts if missing) - a blank
prefix would otherwise scan every key in the namespace across both
workspaces instead of just this one's.}

\item{days}{Number of days to look back.}

\item{namespace_id}{KV namespace ID for \code{SLACK_TOKENS}.}

\item{account_id}{Cloudflare account ID. Defaults to env
\code{CLOUDFLARE_ACCOUNT_ID}.}

\item{api_token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}
}
\value{
A list with \code{days}, \code{entries} (integer count of KV entries
scanned), and \code{totals} (a named integer vector, reaction name to
summed count, sorted descending).
}
\description{
Aggregates the \code{reaction_log:{team_id}:{day}:{reaction}} KV counters
\code{\link[=reaction_log_increment]{reaction_log_increment()}} writes, across the last \code{days} days.
Backs the \code{"feedback"} command. R port of \code{slash_feedback()} from
the deleted \code{worker/src/slash-local.js}.
}
