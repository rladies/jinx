% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workspace-mailbox.R
\name{chapter_mailbox_create}
\alias{chapter_mailbox_create}
\title{Create a chapter mailbox in Google Workspace}
\usage{
chapter_mailbox_create(
  city,
  given_name = NULL,
  family_name = NULL,
  base_url = "https://jinx.rladies.org",
  api_key = worker_api_key()
)
}
\arguments{
\item{city}{Chapter city. The worker transliterates and validates it,
and refuses reserved addresses.}

\item{given_name}{Given name on the account. Defaults to \code{"RLadies+"}.}

\item{family_name}{Family name on the account. Defaults to the city.}

\item{base_url}{Worker base URL.}

\item{api_key}{Worker API key. Defaults to the \code{JINX_WORKER_API_KEY}
environment variable, falling back to the older \code{JINX_API_KEY}.}
}
\value{
The created address (invisibly).
}
\description{
Asks the jinx worker to provision \verb{<city>@rladies.org} in the
\verb{/Chapters} organisational unit. The Workspace service account key
lives in the worker rather than in CI, so this call carries only the
worker API key: the most a stolen CI token can do is ask for a
mailbox whose name passes the worker's validation.
}
\details{
The signing and Directory API calls could be done here in R - \code{openssl}
signs RS256 perfectly well - but that would require the service account
key on the Actions runner that runs this function, which is the thing
the current arrangement avoids. \code{worker/src/workspace.js} carries the
full reasoning; it is a deliberate exception to moving worker logic
into this package.

The generated password is never returned. The account is created with
change-password-at-next-login set, and the onboarding team issues the
handover from the Admin console.
}
