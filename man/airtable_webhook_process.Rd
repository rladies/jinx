% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/airtable-invite.R
\name{airtable_webhook_process}
\alias{airtable_webhook_process}
\title{Process an Airtable invite-request webhook}
\usage{
airtable_webhook_process(
  email,
  name = NULL,
  chapter = NULL,
  record_id,
  base_id,
  table_id,
  channel = Sys.getenv("SLACK_COMMUNITY_INVITE_CHANNEL")
)
}
\arguments{
\item{email}{Applicant email.}

\item{name}{Applicant name, or \code{NULL}.}

\item{chapter}{Chapter name, or \code{NULL}.}

\item{record_id}{Airtable record ID.}

\item{base_id}{Airtable base ID.}

\item{table_id}{Airtable table ID.}

\item{channel}{Slack channel to post the request to. Defaults to env
\code{SLACK_COMMUNITY_INVITE_CHANNEL}.}
}
\value{
Invisibly, \code{TRUE} if posted, \code{FALSE} if the base was rejected.
}
\description{
The \code{airtable_webhook} event handler registered in \code{jinx_events()}.
Checks the base is within the configured Airtable token's scope, builds
the approval-request card, and posts it to the community-invite
channel. R port of the remainder of \code{airtable_webhook_handle()} (after
shared-secret verification, which stays in the Worker) from the
deleted \code{worker/src/airtable-invite.js}.
}
