% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channel-copy.R
\name{channel_copy_apply}
\alias{channel_copy_apply}
\title{Apply a channel copy plan}
\usage{
channel_copy_apply(
  plan,
  token,
  dry_run = TRUE,
  skip = character(),
  join = TRUE,
  include_drift = FALSE,
  user_token = NULL
)
}
\arguments{
\item{plan}{A plan from \code{\link[=channel_copy_plan]{channel_copy_plan()}}.}

\item{token}{Slack bot token.}

\item{dry_run}{When \code{TRUE} (the default), report what would be sent
without calling Slack.}

\item{skip}{Channel names to leave alone.}

\item{join}{Join channels the bot is not a member of first. Slack
refuses \code{conversations.setTopic}, \code{setPurpose} and \code{rename} with
\code{not_in_channel} otherwise.}

\item{include_drift}{Also apply rows whose live value has changed
since the review. Off by default: drift means someone edited the
channel after the copy was written, so applying overwrites the newer
text with older reviewed text.}

\item{user_token}{Optional Slack user token used for \code{rename} rows
only. Slack refuses \code{conversations.rename} from a bot token for a
channel the bot did not create; a grant from a workspace owner
passes that check. Topics and descriptions always use \code{token}.}
}
\value{
\code{plan} with \code{applied} and \code{error} columns added.
}
\description{
Only rows with status \code{"apply"} are sent to Slack. Everything else is
returned untouched so the caller keeps a full record of what was
skipped and why.
}
