% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r-challenge.R
\name{r_challenge_render_statement}
\alias{r_challenge_render_statement}
\title{Render a challenge statement as a reprex}
\usage{
r_challenge_render_statement(statement, runner = r_challenge_reprex_runner)
}
\arguments{
\item{statement}{Character scalar of R source stating the challenge.}

\item{runner}{Function taking code lines and returning rendered reprex
output. Defaults to the isolated reprex runner.}
}
\value{
List with \code{ok} (logical) and \code{rendered} (character vector).
}
\description{
Runs the statement (task comment + setup + expected output as literals)
through the reprex runner, producing the rendered block shown to solvers.
Because it runs the statement on its own, a statement that leaks the answer
by calling the not-yet-written function errors out and is rejected.
}
