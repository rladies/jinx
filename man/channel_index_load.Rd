% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/welcome.R
\name{channel_index_load}
\alias{channel_index_load}
\title{Load (and cache) a workspace's full channel name-to-id index}
\usage{
channel_index_load(
  team_id,
  workspace,
  namespace_id = slack_tokens_namespace_id(),
  account_id = Sys.getenv("CLOUDFLARE_ACCOUNT_ID"),
  api_token = Sys.getenv("CLOUDFLARE_API_TOKEN")
)
}
\arguments{
\item{team_id}{Slack team id.}

\item{workspace}{\code{"organiser"} or \code{"community"}.}

\item{namespace_id}{KV namespace ID for \code{SLACK_TOKENS}.}

\item{account_id}{Cloudflare account ID. Defaults to env
\code{CLOUDFLARE_ACCOUNT_ID}.}

\item{api_token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}
}
\value{
A named list mapping channel name to channel id, or \code{NULL} if
the channel list couldn't be fetched.
}
\description{
Callers resolving several channel names at once (e.g.
\code{\link[=welcome_message_render]{welcome_message_render()}}'s starter-channel list) should call this once
and look names up in the result, rather than calling
\code{\link[=slack_channel_id_lookup]{slack_channel_id_lookup()}} per name - each call independently checks the
KV cache, so looking up N names one-by-one means N redundant KV reads
(and N cache-miss races) for data that doesn't change between them.
}
