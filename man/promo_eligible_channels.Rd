% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channel-promo.R
\name{promo_eligible_channels}
\alias{promo_eligible_channels}
\title{Select the community public channels eligible for promotion}
\usage{
promo_eligible_channels(channels, target_channel = NULL, skip = character())
}
\arguments{
\item{channels}{List of Slack channel objects, as returned by
\code{slack_conversations_list()}.}

\item{target_channel}{Name of the channel the spotlight is posted to,
excluded so it never promotes itself.}

\item{skip}{Character vector of additional channel names to exclude.}
}
\value{
A data frame with one row per eligible channel and columns \code{id},
\code{name}, and \code{description} (cleaned of link markup and URLs).
}
\description{
Keeps non-archived public channels that carry a description (Slack
"purpose"), dropping the channel we post the spotlight to and any names in
\code{skip}. The description is what the weekly spotlight is built from, so a
channel with an empty purpose has nothing to promote and is left out.
}
