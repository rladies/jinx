% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chapter-duplicate.R
\name{chapter_duplicate_check}
\alias{chapter_duplicate_check}
\title{Check whether a requested chapter already exists or has neighbours}
\usage{
chapter_duplicate_check(
  city,
  country,
  region = NULL,
  radius_km = 100,
  org = "rladies",
  website_repo = "rladies.github.io"
)
}
\arguments{
\item{city}{Requested chapter city.}

\item{country}{Requested chapter country.}

\item{region}{State/region/province, or \code{NULL}.}

\item{radius_km}{Radius in kilometres for the proximity search. Set to
\code{NULL} to skip geocoding entirely.}

\item{org}{GitHub organization. Defaults to \code{"rladies"}.}

\item{website_repo}{Repository holding \code{data/chapters}.}
}
\value{
A data frame with one row per match and columns \code{city},
\code{country}, \code{status}, \code{email}, \code{meetup}, \code{match}, and \code{distance_km}.
\code{match} is one of \code{"exact"}, \code{"similar"}, or \code{"nearby"}.
}
\description{
Performs the first step of the chapter onboarding checklist: searches
the website's \code{data/chapters} entries for a chapter in the same city,
for chapters with a near-identical city name, and for chapters in the
same country within \code{radius_km} of the requested city.
}
\details{
Only chapters in the requested country, or whose filename contains the
requested city slug, are fetched in full, so the check costs one
directory listing plus a handful of file reads.
}
