% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chapter-groups.R
\name{chapter_meetup_groups}
\alias{chapter_meetup_groups}
\title{Chapters that have a Meetup group, read from the website data}
\usage{
chapter_meetup_groups(
  org = "rladies",
  website_repo = "rladies.github.io",
  statuses = c("active", "prospective")
)
}
\arguments{
\item{org}{GitHub organization. Defaults to \code{"rladies"}.}

\item{website_repo}{Repository holding \code{data/chapters}.}

\item{statuses}{Chapter statuses to include.}
}
\value{
A data frame with columns \code{file}, \code{city}, \code{country},
\code{status}, and \code{urlname}.
}
\description{
The website's \code{data/chapters} entries are the source of truth for
which chapters exist and which Meetup group each uses, so the sync
derives its list from there rather than from a hand-maintained one
that would drift the day it was written.
}
\details{
Retired chapters are excluded by default: they have no upcoming events
and querying them only spends requests.
}
