% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channel-copy.R
\name{channel_copy_plan}
\alias{channel_copy_plan}
\title{Plan the channel copy and rename pass for a workspace}
\usage{
channel_copy_plan(
  workspace = c("organiser", "community"),
  index = NULL,
  proposals = NULL,
  renames = NULL
)
}
\arguments{
\item{workspace}{Either \code{"organiser"} or \code{"community"}.}

\item{index}{Optional pre-fetched channel index, for testing.}

\item{proposals, renames}{Optional proposal data frames, for testing.}
}
\value{
A data frame of planned changes.
}
\description{
Compares the reviewed proposals against the workspace's live state and
classifies every intended change. A row is \code{"unchanged"} when Slack
already holds the proposed value, \code{"drift"} when the live value no
longer matches what the review recorded (someone edited it since, so
the proposal may be stale), \code{"missing"} when the channel is not in the
workspace, and \code{"apply"} otherwise.
}
\details{
Renames are planned last so a rename cannot hide the channel from the
copy rows in the same pass.
}
