% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chapter-team.R
\name{chapter_team_slug}
\alias{chapter_team_slug}
\title{The GitHub team slug for a chapter}
\usage{
chapter_team_slug(urlname, overrides = character(0))
}
\arguments{
\item{urlname}{Meetup urlname, with or without the \verb{rladies-} prefix.}

\item{overrides}{Named character vector mapping a urlname to a slug,
for chapters whose urlname makes a poor team name. Warsaw's is
\code{Spotkania-Entuzjastow-R-Warsaw-R-Users-Group-Meetup}.}
}
\value{
The team slug, or \code{NA_character_} when there is no urlname.
}
\description{
The Meetup urlname, minus the \verb{rladies-} prefix. This is the
convention 108 of the 122 existing chapter teams already follow, and
it is the only chapter field that is unique: seven city names are
shared by more than one chapter, and it is the urlname that separates
them - \code{london} from \code{ldnont}, \code{pdx} from \code{portland-maine}.
}
