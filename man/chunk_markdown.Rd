% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rag-chunk.R
\name{chunk_markdown}
\alias{chunk_markdown}
\title{Chunk markdown into retrieval-sized pieces}
\usage{
chunk_markdown(markdown, meta, target_chars = 1800, min_chars = 200)
}
\arguments{
\item{markdown}{Markdown text (may include YAML frontmatter).}

\item{meta}{Named list with \code{repo}, \code{path}, \code{url}, \code{fallback_title},
optional \code{date}, \code{lastmod} (both unix seconds).}

\item{target_chars}{Approximate target chunk size.}

\item{min_chars}{Minimum chunk size; smaller chunks are dropped.}
}
\value{
List of chunk records (one list per chunk).
}
\description{
Splits a markdown document by H1/H2 sections, then packs paragraphs
into chunks of roughly \code{target_chars} characters. Returns a list of
chunk records with heading, title, and lastmod metadata.
}
