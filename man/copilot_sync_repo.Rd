% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copilot-sync.R
\name{copilot_sync_repo}
\alias{copilot_sync_repo}
\title{Sync grimoire review skills into a repo as Copilot instructions}
\usage{
copilot_sync_repo(
  owner = "rladies",
  repo,
  base = "main",
  branch = "jinx/copilot-review-instructions",
  config = load_copilot_review_config()
)
}
\arguments{
\item{owner}{Target repository owner. Defaults to \code{"rladies"}.}

\item{repo}{Target repository name.}

\item{base}{Base branch. Defaults to \code{"main"}.}

\item{branch}{Working branch. Defaults to
\code{"jinx/copilot-review-instructions"}.}

\item{config}{Config from \code{\link[=load_copilot_review_config]{load_copilot_review_config()}}.}
}
\value{
A list with \code{status} (\code{"created"} or \code{"unchanged"}) and \code{url}
(the PR URL when created).
}
\description{
Fetches the four grimoire review-gate skills (plus the foundation brand
ruleset) and lands them in \code{owner/repo} as
\code{.github/copilot-instructions.md} and path-scoped
\verb{.github/instructions/*.instructions.md} files, via a pull request.
This is what makes GitHub Copilot's code review grimoire-aware. Re-run
whenever grimoire changes to refresh the instructions.
}
