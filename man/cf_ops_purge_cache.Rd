% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cf-ops.R
\name{cf_ops_purge_cache}
\alias{cf_ops_purge_cache}
\title{Purge specific URLs or prefixes from the Cloudflare cache}
\usage{
cf_ops_purge_cache(
  zone_id = Sys.getenv("CLOUDFLARE_ZONE_ID"),
  files = NULL,
  prefixes = NULL,
  token = cf_ops_token()
)
}
\arguments{
\item{zone_id}{Cloudflare zone ID. Defaults to env \code{CLOUDFLARE_ZONE_ID}.}

\item{files}{Optional character vector of URLs to purge.}

\item{prefixes}{Optional character vector of URL prefixes to purge
(without scheme), e.g. \code{"rladies.org/blog"}.}

\item{token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_OPS_API_TOKEN}, falling back to \code{CLOUDFLARE_API_TOKEN}.}
}
\value{
A named list with the purge job \code{id}.
}
\description{
Thin wrapper over \code{\link[cloudflarer:cf_purge_cache]{cloudflarer::cf_purge_cache()}} that deliberately
does not expose \code{purge_everything} — a maintainer who genuinely needs
a full-zone wipe calls \code{cloudflarer::cf_purge_cache(purge_everything = TRUE)} directly. That extra step is intentional friction on the most
dangerous call this integration can make.
}
