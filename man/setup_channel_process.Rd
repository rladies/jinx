% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/welcome.R
\name{setup_channel_process}
\alias{setup_channel_process}
\title{Set up a Slack channel with RLadies+ bookmarks}
\usage{
setup_channel_process(team_id, channel_id, channel_name)
}
\arguments{
\item{team_id}{Slack team id.}

\item{channel_id}{Channel to set up.}

\item{channel_name}{Channel display name, for the reply text.}
}
\value{
Character scalar reply text.
}
\description{
The \code{"setup-channel"} command handler: joins the channel if needed
(public channels only - a private channel requires a human invite
first), then adds any bookmarks from \code{inst/config/bookmarks.json}
the channel doesn't already have. R port of \code{slash_setup_channel()}
from the deleted \code{worker/src/slash-local.js}; reads the bookmarks
config directly from this package's \verb{inst/} instead of the Worker's
redundant GitHub-raw fetch of the same file.
}
