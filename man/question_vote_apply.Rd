% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/question-log.R
\name{question_vote_apply}
\alias{question_vote_apply}
\title{Apply a reaction vote to the question it answered}
\usage{
question_vote_apply(
  team_id,
  item,
  reaction,
  namespace_id = slack_tokens_namespace_id(),
  account_id = Sys.getenv("CLOUDFLARE_ACCOUNT_ID"),
  database_id = "4500d886-2593-44f9-9a01-d38cfa26e8dc",
  api_token = Sys.getenv("CLOUDFLARE_API_TOKEN")
)
}
\arguments{
\item{team_id}{Slack team id.}

\item{item}{Reaction event's \code{item} list: \code{type}, \code{channel}, \code{ts}.}

\item{reaction}{Raw reaction name.}

\item{namespace_id}{KV namespace ID for \code{SLACK_TOKENS}.}

\item{account_id}{Cloudflare account ID. Defaults to env
\code{CLOUDFLARE_ACCOUNT_ID}.}

\item{database_id}{D1 database ID. Defaults to the provisioned
\code{jinx-question-log} database.}

\item{api_token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}
}
\value{
\code{TRUE} if a vote was applied, \code{FALSE} otherwise.
}
\description{
R port of \code{question_vote_apply()} from \code{worker/src/slack-events.js}
(deleted as part of the reaction-handling migration). Looks up the D1
row an answer message links to via the \code{answer_link:{team_id}: {channel}:{ts}} KV key, and increments its up/down count.
}
