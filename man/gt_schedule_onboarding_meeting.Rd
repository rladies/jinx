% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global-team-meeting.R
\name{gt_schedule_onboarding_meeting}
\alias{gt_schedule_onboarding_meeting}
\title{Open the onboarding meeting poll and post it to the onboarding issue}
\usage{
gt_schedule_onboarding_meeting(
  issue_number,
  name,
  team_name,
  org = "rladies",
  repo = "global-team",
  start = Sys.Date() + 7,
  from = "08:00",
  to = "20:00",
  slot = 30,
  tz = "UTC"
)
}
\arguments{
\item{issue_number}{Onboarding issue number the poll link is posted to.}

\item{name}{Full name of the new member (used in the poll title).}

\item{team_name}{Human-readable team name (used in the poll title).}

\item{org}{GitHub organization. Defaults to \code{"rladies"}.}

\item{repo}{Repository holding the onboarding issue. Defaults to
\code{"global-team"}.}

\item{start}{First candidate date. Defaults to one week from today.}

\item{from, to}{Daily availability window in 24h \code{"HH:MM"}. Defaults to a
broad UTC band that overlaps working hours across regions.}

\item{slot}{Slot length in minutes. Defaults to 30.}

\item{tz}{IANA timezone the poll window is painted in. Defaults to
\code{"UTC"} since the global team spans timezones.}
}
\value{
The poll URL (invisibly).
}
\description{
Part of global team onboarding: opens a "find a time" poll on samkoma so
the new member and their team can settle on an onboarding meeting slot.
The poll offers a two-week window of candidate dates starting one week
after it is created, and the poll link is posted as a comment on the
onboarding issue. The poll is created (and the comment posted) under
Jinx's identity.
}
