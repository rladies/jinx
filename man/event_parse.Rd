% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-registry.R
\name{event_parse}
\alias{event_parse}
\title{Parse a \code{slack-event} dispatch payload}
\usage{
event_parse(payload)
}
\arguments{
\item{payload}{A list with \code{kind}, \code{team_id}, \code{response_url}, \code{event}.}
}
\value{
A list with \code{kind}, \code{team_id}, \code{response_url}, \code{event}; \code{kind}
is \code{"unknown"} when the payload's \code{kind} isn't registered.
}
\description{
The Worker's \code{repository_dispatch} payload is already structured JSON
(unlike slash commands, which arrive as free text for \code{\link[=cmd_parse]{cmd_parse()}} to
split), so this is a thin validator rather than a parser: it confirms
\code{kind} is one of the registered \code{jinx_events()} entries and normalizes
the shape callers can rely on.
}
