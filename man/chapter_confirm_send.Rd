% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chapter-confirm.R
\name{chapter_confirm_send}
\alias{chapter_confirm_send}
\title{Confirm a completed onboarding to the chapter organisers}
\usage{
chapter_confirm_send(
  issue_number,
  to,
  city = NULL,
  email = NULL,
  meetup_url = NULL,
  force = FALSE,
  org = "rladies",
  onboarding_repo = "new-chapters-onboarding"
)
}
\arguments{
\item{issue_number}{Onboarding issue number.}

\item{to}{Organiser email addresses.}

\item{city}{Chapter city. Read from the issue when \code{NULL}.}

\item{email}{Chapter email address to quote in the message.}

\item{meetup_url}{Meetup URL to quote in the message.}

\item{force}{Send even with checklist steps outstanding.}

\item{org}{GitHub organization. Defaults to \code{"rladies"}.}

\item{onboarding_repo}{Repository holding onboarding issues.}
}
\value{
The sent message id (invisibly).
}
\description{
Sends the confirmation email from \code{jinx@rladies.org}, cc'ing
\code{chapters@rladies.org} so the onboarding inbox keeps the thread, and
notes on the issue that it went out.
}
\details{
Refuses to send while checklist steps are outstanding, since the email
tells the organisers their chapter is set up. Pass \code{force = TRUE} to
override, which is occasionally right - a step may be recorded
elsewhere - but it should be a decision rather than an accident.

Recipient addresses are passed in rather than read from the issue:
organiser emails are deliberately not stored in the issue metadata,
and this function does not put them back there either. The issue
comment records how many organisers were written to, not who.
}
