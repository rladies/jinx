% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meeting-poll.R
\name{meeting_poll_create}
\alias{meeting_poll_create}
\title{Create a meeting-scheduling poll on samkoma}
\usage{
meeting_poll_create(
  title,
  days,
  from,
  to,
  slot,
  tz = "UTC",
  kind = c("dates", "weekdays"),
  public = TRUE,
  results_hidden = FALSE,
  deadline = NULL,
  base_url = samkoma_base_url()
)
}
\arguments{
\item{title}{Poll title (1-200 characters).}

\item{days}{Character vector of day identifiers: ISO dates
(\code{YYYY-MM-DD}) when \code{kind = "dates"}, or weekday names
(\code{mon}..\code{sun}) when \code{kind = "weekdays"}. 1-60 items.}

\item{from, to}{Start and end of the daily window, \code{"HH:MM"} (24h).}

\item{slot}{Slot length in minutes.}

\item{tz}{IANA timezone name (e.g. \code{"Europe/Oslo"}). Defaults to UTC.}

\item{kind}{Either \code{"dates"} (default) or \code{"weekdays"}.}

\item{public}{Whether the poll is publicly readable. Defaults to \code{TRUE}.}

\item{results_hidden}{Whether responses are hidden from voters.}

\item{deadline}{Optional ISO 8601 datetime after which voting closes.}

\item{base_url}{API base URL.}
}
\value{
A list with \code{id}, \code{url}, and \code{edit_token}.
}
\description{
Creates a "find a time" poll where participants paint their
availability over a set of days and time slots. Polls are public by
default so results can be read back without a stored secret; the
returned \code{edit_token} is the host secret needed to lock the final
slot or read a hidden poll.
}
