% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/authorize.R
\name{cmd_authorize}
\alias{cmd_authorize}
\title{Authorize a parsed command before execution}
\usage{
cmd_authorize(
  command,
  actor = NULL,
  source = c("github", "slack"),
  api_key = Sys.getenv("AIRTABLE_API_KEY")
)
}
\arguments{
\item{command}{Parsed command list from \code{\link[=cmd_parse]{cmd_parse()}}, or \code{NULL}.}

\item{actor}{The requesting actor: a GitHub login when \code{source} is
\code{"github"}, or a Slack username when \code{source} is \code{"slack"}.}

\item{source}{Origin of the command, \code{"github"} or \code{"slack"}.}

\item{api_key}{Airtable API key used for the directory lookup.}
}
\value{
A list with \code{ok} (logical) and \code{message} (a refusal string
when \code{ok} is \code{FALSE}, otherwise \code{NULL}).
}
\description{
Privileged commands (anything not in \code{safe_commands()}) may only be
run by members of the global team, identified via the Airtable member
directory. Read-only commands are always allowed. The check fails
closed: an unknown actor is denied, and a directory lookup error is
also denied (with a distinct "try again" message).
}
