% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r-challenge-draft.R
\name{r_challenge_draft_post}
\alias{r_challenge_draft_post}
\title{Draft, verify, and propose a weekly R challenge}
\usage{
r_challenge_draft_post(
  difficulty = NULL,
  repo = "rladies/cauldron",
  max_tries = 3L,
  account_id = Sys.getenv("CLOUDFLARE_ACCOUNT_ID"),
  api_token = Sys.getenv("CLOUDFLARE_API_TOKEN"),
  model = workers_ai_chat_model()
)
}
\arguments{
\item{difficulty}{Optional difficulty to request; \code{NULL} lets the model
choose.}

\item{repo}{Target review repo as \code{"owner/name"}.}

\item{max_tries}{Maximum generate-and-verify attempts before giving up.}

\item{account_id}{Cloudflare account ID. Defaults to env
\code{CLOUDFLARE_ACCOUNT_ID}.}

\item{api_token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}

\item{model}{Workers AI chat model.}
}
\value{
Invisibly, \code{TRUE} if a challenge was proposed, else \code{FALSE}.
}
\description{
Top-level entry for the draft cron: builds a fully verified challenge with
\code{\link[=r_challenge_draft_build]{r_challenge_draft_build()}} and, if one passes every gate, opens it as a
\code{status: proposed} issue via \code{\link[=r_challenge_open_issue]{r_challenge_open_issue()}} for an organiser to
review. Does nothing (and does not error) when no challenge survives
verification, so a scheduled run never proposes unverified output.
}
