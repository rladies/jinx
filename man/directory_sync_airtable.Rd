% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/directory-airtable.R
\name{directory_sync_airtable}
\alias{directory_sync_airtable}
\title{Sync directory entries from Airtable}
\usage{
directory_sync_airtable(
  base_id = directory_base_id(),
  api_key = Sys.getenv("AIRTABLE_API_KEY"),
  org = "rladies",
  directory_repo = "directory"
)
}
\arguments{
\item{base_id}{Airtable base ID. Defaults to the directory base.}

\item{api_key}{Airtable API key. Defaults to \code{AIRTABLE_API_KEY} env var.}

\item{org}{GitHub organization. Defaults to \code{"rladies"}.}

\item{directory_repo}{Directory repository name.}
}
\value{
PR URL if changes found, \code{NULL} otherwise (invisibly).
}
\description{
Fetches directory submissions from Airtable, transforms each eligible
record into the directory entry schema, and opens (or updates) a PR on
the directory repo containing only the entries whose content changed.
}
\details{
Only submissions marked \code{minority_gender == "yes"} and not flagged as a
\verb{DEFAULT ROW} are processed. Returning submitters are matched to their
existing entry by slug (\code{directory_id}, falling back to \code{identifier}), and
their submission is merged onto the existing file: fields listed in
\code{clear_fields} are wiped first, then submitted fields overlay the rest, so
a partial update never drops data the submitter left blank.

Delete requests are collected and reported in the PR body but are \strong{not}
executed here; destructive removal stays with the reviewed purge workflow.
}
