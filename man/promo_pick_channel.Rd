% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channel-promo.R
\name{promo_pick_channel}
\alias{promo_pick_channel}
\title{Pick the next channel to spotlight, round-robin}
\usage{
promo_pick_channel(eligible, recent = character())
}
\arguments{
\item{eligible}{Data frame from \code{\link[=promo_eligible_channels]{promo_eligible_channels()}}.}

\item{recent}{Character vector of channel ids featured earlier this cycle.}
}
\value{
\code{NULL} when nothing is eligible, otherwise a list with \code{channel}
(the chosen single-row data frame) and \code{recent} (the updated vector to
persist).
}
\description{
Chooses the alphabetically-first eligible channel that has not been
featured since the cycle began, so every channel gets a turn before any
repeats. \code{recent} is pruned to currently-eligible ids first (channels that
were archived or lost their description drop out), and the cycle resets
once every eligible channel has been seen.
}
