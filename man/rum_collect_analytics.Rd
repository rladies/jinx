% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/website-rum.R
\name{rum_collect_analytics}
\alias{rum_collect_analytics}
\title{Collect Cloudflare RUM (Web Analytics) traffic data}
\usage{
rum_collect_analytics(
  account_id = Sys.getenv("CLOUDFLARE_ACCOUNT_ID"),
  site_tag = Sys.getenv("CLOUDFLARE_RUM_SITE_TAG"),
  token = Sys.getenv("CLOUDFLARE_API_TOKEN"),
  since = Sys.Date() - 30,
  until = Sys.Date()
)
}
\arguments{
\item{account_id}{Cloudflare account ID. Defaults to env
\code{CLOUDFLARE_ACCOUNT_ID}.}

\item{site_tag}{RUM site tag, as returned by
\code{cloudflarer::cf_list_rum_sites()}. Defaults to env
\code{CLOUDFLARE_RUM_SITE_TAG}.}

\item{token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}

\item{since, until}{Date range, half-open \verb{[since, until)}. Defaults to
the last 30 days.}
}
\value{
Named list with \code{site_tag}, \code{since}, \code{until}, \code{pageviews},
\code{top_pages}, \code{top_sources}, and \code{top_countries}.
}
\description{
Queries the Cloudflare Web Analytics beacon data for a date range,
via \code{\link[cloudflarer:cf_rum_page_views]{cloudflarer::cf_rum_page_views()}} and \code{\link[cloudflarer:cf_rum_top]{cloudflarer::cf_rum_top()}}.
A sibling to \code{\link[=website_collect_analytics]{website_collect_analytics()}} (which queries Plausible) —
Cloudflare RUM is a separate, beacon-based data source.
}
