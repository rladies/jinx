% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r-challenge.R
\name{r_challenge_draft_build}
\alias{r_challenge_draft_build}
\title{Draft a fully verified weekly R challenge}
\usage{
r_challenge_draft_build(
  difficulty = NULL,
  max_tries = 3L,
  account_id = Sys.getenv("CLOUDFLARE_ACCOUNT_ID"),
  api_token = Sys.getenv("CLOUDFLARE_API_TOKEN"),
  model = workers_ai_chat_model()
)
}
\arguments{
\item{difficulty}{Optional difficulty to request; \code{NULL} lets the model
choose.}

\item{max_tries}{Maximum generate-and-verify attempts before giving up.}

\item{account_id}{Cloudflare account ID. Defaults to env
\code{CLOUDFLARE_ACCOUNT_ID}.}

\item{api_token}{Cloudflare API token. Defaults to env
\code{CLOUDFLARE_API_TOKEN}.}

\item{model}{Workers AI chat model.}
}
\value{
A list with \code{challenge}, \code{statement} (rendered statement reprex),
\code{reprex} (solution verification output), \code{critique}, and \code{attempts}, or
\code{NULL} if no verified challenge was produced.
}
\description{
Orchestrates the full generator: draft with Workers AI, parse, verify the
reference solution with reprex, render the statement as a reprex, then
adversarially review. Regenerates on any failed gate, up to \code{max_tries}, and
returns \code{NULL} if none passes, so a scheduled run never emits unverified
output.
}
